import React from "react";
import { StatsSummary } from "./statsComponents/statsSummary";

export const Stats = ({ films }) => {
  return <section className="statistic">{/*<StatsSummary />*/}</section>;
};
